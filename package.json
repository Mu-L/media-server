{
	"name": "medooze-media-server-src",
	"version": "3.1.8",
	"description": "Redistributable Source for Medooze Media Server",
	"main": "index.js",
	"files": [
		"package.json",
		"index.js",
		"README.md",
		"src/",
		"include/",
		"ext/"
	],
	"scripts": {
		"test": "mkdir -p build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Debug && cmake --build . -j 8 &&  ./MediaServerUnitTest --gtest_output=xml:../test_results.xml && ./MediaServerLegacyTest"
	},
	"repository": "https://github.com/medooze/media-server.git"
}
